# Visual Wiring Diagrams

## Raspberry Pi Pico + SSD1306 OLED (RECOMMENDED)

```
┌─────────────────────────────────────┐
│    Raspberry Pi Pico (RP2040)       │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  USB                        │   │
│  └─────────────────────────────┘   │
│                                     │
│  GP0 (SDA) ●────────────────────┐  │
│  GP1 (SCL) ●─────────────────┐  │  │
│  3.3V      ●──────────────┐  │  │  │
│  GND       ●───────────┐  │  │  │  │
│                        │  │  │  │  │
└────────────────────────┼──┼──┼──┼──┘
                         │  │  │  │
                         │  │  │  │
      ┌──────────────────┘  │  │  │
      │  ┌───────────────────┘  │  │
      │  │  ┌────────────────────┘  │
      │  │  │  ┌─────────────────────┘
      │  │  │  │
      ▼  ▼  ▼  ▼
    ┌──────────────┐
    │   SSD1306    │
    │ 128x64 OLED  │
    │              │
    │  GND VCC SCL │
    │   │   │   │  │
    │   ○   ○   ○  │ SDA
    │              │  ○
    └──────────────┘
```

**Connections:**
```
Pico GP0  → OLED SDA (Data)
Pico GP1  → OLED SCL (Clock)
Pico 3.3V → OLED VCC (Power)
Pico GND  → OLED GND (Ground)
```

---

## Arduino Uno + SSD1306 OLED

```
┌─────────────────────────────────────┐
│        Arduino Uno (ATmega328)      │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  USB-B                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  A4 (SDA) ●─────────────────────┐  │
│  A5 (SCL) ●──────────────────┐  │  │
│  5V       ●───────────────┐  │  │  │
│  GND      ●────────────┐  │  │  │  │
│                        │  │  │  │  │
└────────────────────────┼──┼──┼──┼──┘
                         │  │  │  │
                         │  │  │  │
      ┌──────────────────┘  │  │  │
      │  ┌───────────────────┘  │  │
      │  │  ┌────────────────────┘  │
      │  │  │  ┌─────────────────────┘
      │  │  │  │
      ▼  ▼  ▼  ▼
    ┌──────────────┐
    │   SSD1306    │
    │ 128x64 OLED  │
    │              │
    │  GND VCC SCL │
    │   │   │   │  │
    │   ○   ○   ○  │ SDA
    │              │  ○
    └──────────────┘
```

**Connections:**
```
Uno A4  → OLED SDA (Data)
Uno A5  → OLED SCL (Clock)
Uno 5V  → OLED VCC (Power) ⚠️ Check if 5V tolerant!
Uno GND → OLED GND (Ground)
```

---

## ESP32 DevKit + SSD1306 OLED

```
┌─────────────────────────────────────┐
│        ESP32 DevKit (Dual Core)     │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  micro-USB                  │   │
│  └─────────────────────────────┘   │
│                                     │
│  GPIO21 (SDA) ●─────────────────┐  │
│  GPIO22 (SCL) ●──────────────┐  │  │
│  3.3V         ●───────────┐  │  │  │
│  GND          ●────────┐  │  │  │  │
│                        │  │  │  │  │
└────────────────────────┼──┼──┼──┼──┘
                         │  │  │  │
                         │  │  │  │
      ┌──────────────────┘  │  │  │
      │  ┌───────────────────┘  │  │
      │  │  ┌────────────────────┘  │
      │  │  │  ┌─────────────────────┘
      │  │  │  │
      ▼  ▼  ▼  ▼
    ┌──────────────┐
    │   SSD1306    │
    │ 128x64 OLED  │
    │              │
    │  GND VCC SCL │
    │   │   │   │  │
    │   ○   ○   ○  │ SDA
    │              │  ○
    └──────────────┘
```

**Connections:**
```
ESP32 GPIO21 → OLED SDA (Data)
ESP32 GPIO22 → OLED SCL (Clock)
ESP32 3.3V   → OLED VCC (Power)
ESP32 GND    → OLED GND (Ground)
```

---

## Breadboard Layout (Any Board)

```
    Power Rails
    ───────────
    + (Red)                     Microcontroller
    - (Blue)                    ┌──────────┐
                                │          │
    ●═══════════════════════════●  5V/3.3V │
    │                           │          │
    ●═══════════════════════════●  GND     │
    │                           │          │
    │                           │  SDA     ●═══════╗
    │                           │          │       ║
    │                           │  SCL     ●═══╗   ║
    │                           │          │   ║   ║
    │                           └──────────┘   ║   ║
    │                                          ║   ║
    │                                          ║   ║
    │         ┌──────────────┐                 ║   ║
    │         │   SSD1306    │                 ║   ║
    │         │ 128x64 OLED  │                 ║   ║
    │         │              │                 ║   ║
    │         │  GND VCC SCL SDA               ║   ║
    │         │   │   │   │  │                 ║   ║
    ╰─────────●───╯   │   ●══╝                 ║   ║
              │       │   │                    ║   ║
              ╰───────●───╯                    ║   ║
                      │                        ║   ║
                      ╰════════════════════════╝   ║
                                                   ║
                          ╰════════════════════════╝

Legend:
  ═══  Jumper wire
  ●    Connection point
  ║    Jumper wire (crossing)
```

---

## Minimal Wiring (No Breadboard)

**Direct connection with 4 jumper wires:**

```
OLED Display         Jumper Wires        Microcontroller
┌────────────┐                          ┌──────────┐
│            │                          │          │
│   [████]   │                          │  [USB]   │
│            │                          │          │
│  ○ ○ ○ ○   │                          │          │
│  │ │ │ │   │                          │  ● ● ●   │
│  │ │ │ └───┼──── Red wire ────────────┼──┤ 5V│   │
│  │ │ └─────┼──── Yellow wire ─────────┼──┤SDA│   │
│  │ └───────┼──── Green wire ──────────┼──┤SCL│   │
│  └─────────┼──── Black wire ──────────┼──┤GND│   │
│            │                          │          │
└────────────┘                          └──────────┘
  GND VCC SCL SDA
```

**Color coding suggestion:**
- 🔴 **Red** = VCC (Power, 3.3V or 5V)
- ⚫ **Black** = GND (Ground)
- 🟢 **Green** = SCL (I2C Clock)
- 🟡 **Yellow** = SDA (I2C Data)

---

## I2C Bus with Multiple Devices

**You can connect multiple I2C devices on the same bus:**

```
                  Microcontroller
                  ┌──────────┐
                  │          │
    Pull-up       │  3.3V/5V ●───────┐
    Resistors     │          │       │
    4.7kΩ         │  GND     ●───┐   │
    │ │           │          │   │   │
    ↓ ↓           │  SDA     ●───┼───┼─── SDA bus ────┐
    ┌┐┌┐          │          │   │   │                │
    ││││          │  SCL     ●───┼───┼─── SCL bus ─┐  │
    ││││          │          │   │   │             │  │
    └┘└┘          └──────────┘   │   │             │  │
    │ │                          │   │             │  │
    ╰─┴─────────── SDA bus ──────┼───┼─────────────┤  │
      │                          │   │             │  │
      ╰─────────── SCL bus ──────┤   │             │  │
                                 │   │             │  │
    ┌──────────┐                 │   │             │  │
    │ SSD1306  │                 │   │             │  │
    │ 0x3C     │                 │   │             │  │
    └──────────┘                 │   │             │  │
    GND VCC SCL SDA              │   │             │  │
     │   │   │   │               │   │             │  │
     ╰───┼───●───●───────────────┘   │             │  │
         ╰───────────────────────────┘             │  │
                                                   │  │
    ┌──────────┐                                   │  │
    │ Other    │                                   │  │
    │ I2C      │                                   │  │
    │ Device   │                                   │  │
    └──────────┘                                   │  │
    GND VCC SCL SDA                                │  │
     │   │   │   │                                 │  │
     ╰───┼───●───●─────────────────────────────────┘  │
         ╰──────────────────────────────────────────┘
```

**Notes:**
- All devices share the same SDA and SCL lines
- Each device must have a unique I2C address
- Only one pair of pull-up resistors needed for the entire bus
- Connect VCC and GND to each device

---

## Troubleshooting Diagram

**If display doesn't work, check these connections:**

```
1. Power Connection
   ┌───────┐
   │ OLED  │
   └───────┘
     │  │
     V  V
   VCC GND
     │  │
     │  └──> Should be 0V (ground)
     └─────> Should be 3.3V or 5V
   
   📏 Use multimeter to verify!

2. I2C Connection  
   ┌───────┐
   │ OLED  │
   └───────┘
     │  │
     V  V
   SDA SCL
     │  │
     └──┴──> Should see changing voltage
             when data is transmitted
   
   Check with logic analyzer or oscilloscope

3. Address Check
   Most common: 0x3C
   Alternative:  0x3D
   
   Run I2C scan code to detect address!
```

---

## Physical Layout Tips

**Best Practices:**
1. ✅ Keep wires short (< 6 inches)
2. ✅ Twist SDA and SCL wires together
3. ✅ Separate power wires from I2C wires
4. ✅ Use quality jumper wires
5. ✅ Secure OLED to prevent stress on pins

**Avoid:**
1. ❌ Wires longer than 12 inches without shielding
2. ❌ Running I2C wires parallel to power wires
3. ❌ Using damaged or loose jumper wires
4. ❌ Bending OLED pins repeatedly
5. ❌ Mixing 3.3V and 5V incorrectly

---

## Testing Connection

**Quick hardware test:**

```
1. Power only (no code running)
   ┌───────┐
   │ OLED  │ Should show faint glow
   └───────┘ or test pattern

2. I2C scan
   Run I2C scanner code
   Should detect address 0x3C or 0x3D

3. Simple display test
   Flash "hello world" example
   Should see text on display
```

---

**Need more help?** See `SSD1306_PIN_GUIDE.md` for detailed pin information!
